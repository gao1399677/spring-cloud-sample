spring:
  application:
    name: department-service
  cloud:
    config:
      uri: http://localhost:8088
    stream:
      bindings:
        foodOrdersChannel:
          destination: foodOrders
      default:
        contentType: application/json