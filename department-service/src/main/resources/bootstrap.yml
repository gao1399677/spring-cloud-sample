spring:
  application:
    name: department-service
  cloud:
    config:
      uri: http://localhost:8088
    stream:
      bindings:
        output:
          destination: foodOrders
      default:
        contentType: application/json